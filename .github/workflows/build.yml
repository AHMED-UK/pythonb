name: Build Python 3.13.14 for Android

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-24.04
    container:
      image: excedrin/adv:latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64-v8a
            triple: aarch64-linux-android
            api: 35
            lib_suffix: 64
          - arch: armeabi-v7a
            triple: armv7a-linux-androideabi
            api: 35
            lib_suffix: ""
          - arch: x86_64
            triple: x86_64-linux-android
            api: 35
            lib_suffix: 64
          - arch: x86
            triple: i686-linux-android
            api: 35
            lib_suffix: ""

    steps:
      - name: Install Build Essentials
        run: |
          apt-get update
          apt-get install -y build-essential pkg-config curl xz-utils git

      - name: Download and Extract Python 3.13.14
        run: |
          mkdir python-src
          curl -L https://github.com/zrsx/cpython3/releases/download/v3.13.14/Python-3.13.14.tar.xz -o python.tar.xz
          tar -xf python.tar.xz -C python-src --strip-components=1

      - name: Build Host Python
        run: |
          cd python-src
          mkdir build-host
          cd build-host
          ../configure --prefix=$(pwd)/install
          make -j$(nproc)
          make install
          echo "HOST_PYTHON=$(pwd)/install/bin/python3.13" >> $GITHUB_ENV

      - name: Cross-Compile Python for Android (${{ matrix.arch }})
        run: |
          cd python-src
          mkdir -p build-android-${{ matrix.arch }}
          cd build-android-${{ matrix.arch }}

          # 1. Setup Toolchain
          export CC=${{ matrix.triple }}${{ matrix.api }}-clang
          export CXX=${{ matrix.triple }}${{ matrix.api }}-clang++
          export AR="llvm-ar"
          export NM="llvm-nm"
          export RANLIB="llvm-ranlib"
          export STRIP="llvm-strip"
          export READELF="llvm-readelf"

          # 2. Define Paths (Correcting lib vs lib64)
          export PREFIX="/opt/python3.13-android/${{ matrix.arch }}"
          export SYS_INC="/system/include"
          # Use lib64 for 64-bit archs, lib for 32-bit
          export SYS_LIB_BASE="/system/lib${{ matrix.lib_suffix }}"
          export SYS_LIB_TRIPLE="${SYS_LIB_BASE}/${{ matrix.triple }}"

          # 3. Environment Variables
          # We set PKG_CONFIG_LIBDIR to prevent it from picking up host (Ubuntu) libraries
          export PKG_CONFIG_LIBDIR="${SYS_LIB_TRIPLE}/pkgconfig:${SYS_LIB_BASE}/pkgconfig"
          export CFLAGS="-fPIC"
          export CPPFLAGS="-I${SYS_INC} -I${SYS_INC}/ncursesw -DNCURSES_WIDECHAR"
          export LDFLAGS="-L${SYS_LIB_TRIPLE} -L${SYS_LIB_BASE} -Wl,-rpath=/system/lib${{ matrix.lib_suffix }} -Wl,--enable-new-dtags"
          
          # Module specific flags for Python 3.13
          export LIBMPDEC_CFLAGS="-I${SYS_INC}"
          export LIBMPDEC_LIBS="-L${SYS_LIB_TRIPLE} -lmpdec"
          export LIBSSL_CFLAGS="-I${SYS_INC}"
          export LIBSSL_LIBS="-L${SYS_LIB_TRIPLE} -lssl -lcrypto"
          export SQLITE3_CFLAGS="-I${SYS_INC}"
          export SQLITE3_LIBS="-L${SYS_LIB_TRIPLE} -lsqlite3"
          export LIBFFI_CFLAGS="-I${SYS_INC}"
          export LIBFFI_LIBS="-L${SYS_LIB_TRIPLE} -lffi"
          export LIBLZMA_CFLAGS="-I${SYS_INC}"
          export LIBLZMA_LIBS="-L${SYS_LIB_TRIPLE} -llzma"
          export BZIP2_CFLAGS="-I${SYS_INC}"
          export BZIP2_LIBS="-L${SYS_LIB_TRIPLE} -lbz2"
          export LIBS="-landroid_compat"

          # 4. Configure
          ../configure \
            --host=${{ matrix.triple }} \
            --build=x86_64-pc-linux-gnu \
            --prefix=$PREFIX \
            --with-build-python=$HOST_PYTHON \
            --disable-shared \
            --disable-test-modules \
            --with-system-expat \
            --with-system-libmpdec \
            --with-system-ffi \
            --without-ensurepip \
            --enable-loadable-sqlite-extensions \
            --with-openssl=/system \
            --with-openssl-rpath=auto \
            ac_cv_file__dev_ptmx=yes \
            ac_cv_file__dev_ptc=no \
            ac_cv_func_wcsftime=no \
            ac_cv_func_ftime=no \
            ac_cv_func_faccessat=no \
            ac_cv_func_link=no \
            ac_cv_func_linkat=no \
            ac_cv_buggy_getaddrinfo=no \
            ac_cv_little_endian_double=yes \
            ac_cv_posix_semaphores_enabled=yes \
            ac_cv_func_sem_open=yes \
            ac_cv_func_sem_timedwait=yes \
            ac_cv_func_sem_getvalue=yes \
            ac_cv_func_sem_unlink=yes \
            ac_cv_func_shm_open=yes \
            ac_cv_func_shm_unlink=yes \
            ac_cv_working_tzset=yes \
            ac_cv_header_sys_xattr_h=no \
            ac_cv_func_getgrent=yes \
            ac_cv_func_getpwent=no || { tail -n 100 config.log; exit 1; }

          make -j$(nproc) || { tail -n 100 config.log; exit 1; }
          make install

      - name: Package Build Artifacts
        run: |
          cd /opt/python3.13-android/
          tar -czvf python-3.13.14-android-${{ matrix.arch }}.tar.gz ${{ matrix.arch }}
          mv *.tar.gz $GITHUB_WORKSPACE/

      - name: Upload Python Artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-3.13.14-android-${{ matrix.arch }}
          path: python-3.13.14-android-${{ matrix.arch }}.tar.gz
